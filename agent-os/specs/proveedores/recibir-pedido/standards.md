# Standards: Recibir Pedido

## authentication/role-middleware

`role:<value>` middleware. 403 on mismatch.

---

## authentication/session-auth

Laravel session auth.

---

## backend/php-architecture

Route → FormRequest → Controller → ServiceInterface → Service → Model. PHP 8.x conventions.

---

## backend/service-interface

Service + `Contracts/` interface.

---

## frontend/role-namespacing

Views: `resources/views/{role}/{domain}/`.
