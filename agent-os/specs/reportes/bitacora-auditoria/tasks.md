# Tasks — Bitácora de Auditoría

- [x] **Task 1:** Save spec documentation (spec.md, shape.md, standards.md, references.md).
- [x] **Task 2:** Create `AuditoriaAcceso` model + migration (append-only, `cveUsuario` nullable FK, `cDetalles` JSON cast, `fCreado` timestamp, no `fActualizado`). // Existing `App\Models\AuditLog` + migration `2026_04_26_000012_create_audit_logs_table.php` reused. English snake_case columns: `user_id`, `action`, `table_affected`, `record_affected`, `details` (text, JSON-encoded), `ip_address`, `created_at`. No `updated_at` (`UPDATED_AT = null`).
- [x] **Task 3:** Create `App\Services\Bitacora\Contracts\BitacoraServiceInterface` with `log(string $accion, ?int $cveUsuario, ?string $tabla, ?string $registro, array $detalles): void` and `getFiltered(array $filters): LengthAwarePaginator`. // Implemented with English signature: `log(string $action, ?int $userId, ?string $table, ?string $record, array $details): void`.
- [x] **Task 4:** Implement `App\Services\Bitacora\BitacoraService`; register binding in `AppServiceProvider`.
- [x] **Task 5:** Inject `BitacoraServiceInterface` into `AuthController` and log `LOGIN_OK` / `LOGIN_FAIL` (capturing attempted email when user not found) / `LOGOUT`.
- [x] **Task 6:** Inject `BitacoraServiceInterface` into `RecetaController`, `StockController`, `VentaController`, `UserController` and call `log()` at action points (`RECETA_VALIDADA`, `RECETA_RECHAZADA`, `AJUSTE_STOCK`, `VENTA_CANCELADA`, `USUARIO_CREADO`, `ROL_CAMBIADO`). // `AJUSTE_STOCK` injected in `StockService`, `VENTA_CANCELADA` in `SalesController::cancel`, `USUARIO_CREADO` + `ROL_CAMBIADO` in `UsuarioService`. `RECETA_VALIDADA` / `RECETA_RECHAZADA` // out-of-scope (pharmacist module not in this scope).
- [x] **Task 7:** Create `app/Http/Controllers/Web/Reportes/BitacoraController.php` with `index(Request): View` only — no `update`/`destroy`.
- [x] **Task 8:** Register route in `routes/web.php` under `auth` + `role:administrator` middleware: only `GET /admin/reportes/bitacora` → `BitacoraController@index`.
- [x] **Task 9:** Build `resources/views/admin/reportes/bitacora.blade.php`: filter form (usuario / acción / tabla / fechas), paginated table, default last 24h.
- [x] **Task 10:** Verify 403 for non-admin roles (manual check or feature test). // Route gated by `role:administrator` middleware; smoke check via `route:list` confirms middleware stack.
