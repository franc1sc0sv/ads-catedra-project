# Agent OS Standards Index

authentication:
  session-auth:
    description: Single Laravel session-based auth stack — web only, no JWT, no parallel API
  role-middleware:
    description: Route-level role authorization that reads the role from the authenticated user

backend:
  php-architecture:
    description: Layer structure (Route→FormRequest→Controller→Service→Model→Resource), file placement, PHP 8.x conventions
  service-interface:
    description: Every service has a Contracts/ interface; controllers inject the interface, never the concrete class

frontend:
  role-namespacing:
    description: Each role owns its full slice (routes, controller, views, nav) — checklist for adding a new role
